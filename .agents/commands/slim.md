---
description: /rust-skills:rust slim pin
---


# /slim

Pin. Load the rust skill and run `reference/slim.md` on `$ARGUMENTS`.
构建减肥与文件卫生：timings 定位、裁依赖；过期开发文件/target 分层清理. Args `[target]`. Write policy is SKILL's — this pin cannot widen writes.
Equiv: `/rust-skills:rust slim $ARGUMENTS`.
