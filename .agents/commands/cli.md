---
description: /rust-skills:rust cli pin
---


# /cli

Pin. Load the rust skill and run `reference/cli.md` on `$ARGUMENTS`.
clap 4.6 CLI：derive、子命令、env、退出码、补全；解析只在 bin. Args `[target]`. Write policy is SKILL's — this pin cannot widen writes.
Equiv: `/rust-skills:rust cli $ARGUMENTS`.
