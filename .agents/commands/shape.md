---
description: /rust-skills:rust shape pin
---


# /shape

Pin. Load the rust skill and run `reference/shape.md` on `$ARGUMENTS`.
写码前设计：落点/类型/错误/并发四问，只出一页设计小结. Args `<feature>`. Write policy is SKILL's — this pin cannot widen writes.
Equiv: `/rust-skills:rust shape $ARGUMENTS`.
