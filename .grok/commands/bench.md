---
description: /rust-skills:rust bench pin
---


# /bench

Pin. Load the rust skill and run `reference/bench.md` on `$ARGUMENTS`.
性能纪律：高层 SQL/HTTP/锁先于火焰图；同机前后对比；测→看 self→改一处→墙钟. Args `<target>`. Write policy is SKILL's — this pin cannot widen writes.
Equiv: `/rust-skills:rust bench $ARGUMENTS`.
