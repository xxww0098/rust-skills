---
description: /rust-skills:rust gate pin
---


# /gate

Pin. Load the rust skill and run `reference/gate.md` on `$ARGUMENTS`.
生成/维护 xtask 门禁与 clippy 基线（只收紧不放宽）. Args ``. Write policy is SKILL's — this pin cannot widen writes.
Equiv: `/rust-skills:rust gate $ARGUMENTS`.
