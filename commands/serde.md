---
description: /rust-skills:rust serde pin
---


# /serde

Pin. Load the rust skill and run `reference/serde.md` on `$ARGUMENTS`.
序列化边界：零拷贝、enum 表示、字段纪律、兼容演进. Args `[target]`. Write policy is SKILL's — this pin cannot widen writes.
Equiv: `/rust-skills:rust serde $ARGUMENTS`.
