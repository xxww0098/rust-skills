---
description: /rust-skills:rust init pin
---


# /init

Pin. Load the rust skill and run `reference/init.md` on `$ARGUMENTS`.
把工程调和到最小基线并生成/刷新 RUST.md 画像；新项目从这里开始. Args ``. Write policy is SKILL's — this pin cannot widen writes.
Equiv: `/rust-skills:rust init $ARGUMENTS`.
