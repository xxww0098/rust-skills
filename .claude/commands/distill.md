---
description: /rust-skills:rust distill pin
---

# /distill

Pin. Load the rust skill and run `reference/distill.md` on `$ARGUMENTS`.
旧代码优化入口：删抽象、结构梯子、crate 只建议不擅迁. Args `[target]`. Write policy is SKILL's — this pin cannot widen writes.
Equiv: `/rust-skills:rust distill $ARGUMENTS`.
