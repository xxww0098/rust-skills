---
description: /rust-skills:rust modernize pin
---

# /modernize

Pin. Load the rust skill and run `reference/modernize.md` on `$ARGUMENTS`.
把过时写法换成现代等价物（lazy_static → OnceLock 等）. Args `[target]`. Write policy is SKILL's — this pin cannot widen writes.
Equiv: `/rust-skills:rust modernize $ARGUMENTS`.
