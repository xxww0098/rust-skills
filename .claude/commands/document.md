---
description: /rust-skills:rust document pin
---

# /document

Pin. Load the rust skill and run `reference/document.md` on `$ARGUMENTS`.
从当前项目事实生成/刷新 RUST.md 画像；老项目先跑这个. Args ``. Write policy is SKILL's — this pin cannot widen writes.
Equiv: `/rust-skills:rust document $ARGUMENTS`.
