---
description: /rust-skills:rust capture pin
---

# /capture

Pin. Load the rust skill and run `reference/capture.md` on `$ARGUMENTS`.
把踩坑蒸馏进项目 outbox，人工确认后提升为规则. Args `[lesson]`. Write policy is SKILL's — this pin cannot widen writes.
Equiv: `/rust-skills:rust capture $ARGUMENTS`.
