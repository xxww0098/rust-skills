# Scene 7: capture writes project outbox only.

Lesson: CString pointer escaped into C after `as_ptr()` on a temporary.

Anti-pattern: edit installed plugin cache `rules-full.md` or auto-commit.
Required path: `.rust-skills/capture-outbox.md`
Promote only with explicit `capture promote` in the skill source repo.
