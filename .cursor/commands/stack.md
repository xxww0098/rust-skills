---
description: /rust-skills:rust stack pin
---


# /stack

Pin. Load the rust skill and run `reference/stack.md` on `$ARGUMENTS`.
分析仓库与口述产物，推荐并（仅 --apply/「改」）按表给缺失层加依赖；不删活栈. Args `[target] [--apply]`. Write policy is SKILL's — this pin cannot widen writes.
Equiv: `/rust-skills:rust stack $ARGUMENTS`.
