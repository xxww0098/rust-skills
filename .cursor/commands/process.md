---
description: /rust-skills:rust process pin
---


# /process

Pin. Load the rust skill and run `reference/process.md` on `$ARGUMENTS`.
多进程选型与编排：隔离/故障域、Command 生命周期、fork 边界、进程池、IPC、信号停机. Args `[target]`. Write policy is SKILL's — this pin cannot widen writes.
Equiv: `/rust-skills:rust process $ARGUMENTS`.
